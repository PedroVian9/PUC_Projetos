#include <iostream>
 
int main()
 {
	int fat = 1;
	int N = 0;
	std::cout<<"Digite um número inteiro para calcular: "<<'\n';
	std::cin>>N;
	
	for (int aux = N; aux > 1 ; aux = aux - 1)
	{
		fat *= aux;
	}
	
	std::cout<<"O valor fatorial é: "<<fat<<'\n';		
}
